# deps
node_modules/
.pnp
.pnp.js

# next
.next/
out/
build/

# env — never commit, always allow .env.example as the template
.env
.env.*
!.env.example

# convex
convex/_generated/

# claude worktrees
.claude/worktrees/
.claude/settings.local.json

# agent skills — auto-installed by ./setup-skills.sh, never commit
# (`.*/skills/` covers .claude/skills, .cursor/skills, .codex/skills, etc.
# `.agents/` is the universal location used by skills CLI's `-a '*'` flag)
.agents/
.*/skills/

# graphify knowledge graph — generated per-project, never committed
graphify-out/

# test outputs
coverage/
playwright-report/
test-results/
.vitest-cache/

# build caches
.turbo/
.swc/

# editor / os
.DS_Store
.idea/
*.swp
*.swo

# misc
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.vercel
*.tsbuildinfo
next-env.d.ts
